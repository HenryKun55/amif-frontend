import tw, { styled } from 'twin.macro'

export const Container = styled.div(tw`w-full flex flex-col`)

export const Title = styled.span(tw``)

export const Value = styled.button(tw``)

export const OtherValues = styled.input(tw``)
