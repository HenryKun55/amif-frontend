import tw, { styled } from 'twin.macro'

export const Container = styled.div(() => [tw`w-full flex align-middle `])

export const Logo = styled.div([tw`flex w-2/12`])

export const Image = styled.img([tw`w-28`])

export const Content = styled.div([tw`flex w-8/12`])

export const Donate = styled.div([tw`flex w-2/12`])
