import tw, { styled } from 'twin.macro'

export const Container = styled.div([
  tw`w-full h-full flex flex-col overflow-y-auto `,
])

export const Events = styled.div([
  tw`flex w-full gap-5 p-5 flex-wrap justify-center`,
])
